{% style %}
  .student-dashboard__content {
    {% include 'section_style_card_alignment' %}
  }
{% endstyle %}

{% schema %}
{
  "label": "Default",
  "description": "This is a default section — the foundation of your Thinkific site.  While this section has limited customization, you can build out this page by adding new sections.",
  "settings": [
    {
      "type": "radio",
      "id": "card_alignment",
      "label": "Card alignment",
      "description": "",
      "default": "left",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ]
    }
  ]
}
{% endschema %}
